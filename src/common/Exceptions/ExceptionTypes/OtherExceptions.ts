export enum OtherExceptions {
  InvalidQueryParameter = 'Wrong query parameters',
  PermissionExpired = 'Permission has been expired',
  PermissionOwnerAlready = 'User already have highest role',
}

export enum ValidationExceptions {
  ValidationException = 'Validation error',
}
